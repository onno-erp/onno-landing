# onno landing

Landing page for [onno](https://onno.su).

## Development

```sh
npm install
npm run dev
```

## Production image

Pushes to `main` publish `ghcr.io/onno-erp/onno-landing`. Flux deploys timestamped image tags to `onno.su`.
